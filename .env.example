# Komma-separiert. Im Dev-Mode (ohne API_KEYS) gilt automatisch der Key "dev".
API_KEYS=dev,mysecretkey123

# Optional: Pfad zum FastText-Modell (falls du es nutzen willst)
FASTTEXT_MODEL_PATH=models/lid.176.ftz

# Optional: Redis für Rate-Limit in Produktion
# REDIS_URL=redis://default:pass@host:6379/0

# Requests pro Minute pro API-Key
LIMIT_PER_MINUTE=120
